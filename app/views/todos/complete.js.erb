
$('div#<%= dom_id(@todo) %>').parent().slideUp();
$('.completed > ul ').prepend("<%= j render partial: 'todo', locals: {todo: @todo} %>");
$('div#<%= dom_id(@todo) %> > div').find('.todo-text').addClass('.completed');
